const header=document.querySelector('[data-header]');const menuButton=document.querySelector('[data-menu-toggle]');const mobileNav=document.querySelector('[data-mobile-nav]');menuButton?.addEventListener('click',()=>{mobileNav?.classList.toggle('is-open');document.body.classList.toggle('menu-open')});document.querySelectorAll('a[href^="#"]').forEach(link=>{link.addEventListener('click',event=>{const target=document.querySelector(link.getAttribute('href'));if(!target)return;event.preventDefault();mobileNav?.classList.remove('is-open');document.body.classList.remove('menu-open');const offset=header?.offsetHeight||0;window.scrollTo({top:target.getBoundingClientRect().top+window.scrollY-offset,behavior:'smooth'})})});const cards=[...document.querySelectorAll('.case-card')];let activeCase=0;function showCase(index){if(!cards.length)return;activeCase=(index+cards.length)%cards.length;cards.forEach((card,i)=>card.classList.toggle('is-active',i===activeCase))}document.querySelector('[data-case-prev]')?.addEventListener('click',()=>showCase(activeCase-1));document.querySelector('[data-case-next]')?.addEventListener('click',()=>showCase(activeCase+1));showCase(0);
